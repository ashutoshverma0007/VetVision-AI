# AI Workspace

This folder is reserved for inference, model orchestration, training utilities, explainability,
and experiment code built around PyTorch, OpenCV, Ultralytics YOLOv11, XGBoost, and SHAP.
The scaffold keeps it separate from the web API so machine-learning work can evolve independently.
