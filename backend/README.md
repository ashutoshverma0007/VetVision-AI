# Backend

FastAPI service scaffold for VetVision AI.

This workspace contains the HTTP API surface, configuration loading, database session wiring,
Pydantic response models, and Alembic migration structure. It intentionally avoids feature logic.
